//包仓库声明
ext {
    //声明只用京东的仓库
    def repository = "http://maven.gardenerframework.io/artifactory"
    snapshots = "${repository}/libs-snapshot" as String
    releases = "${repository}/libs-release" as String
}