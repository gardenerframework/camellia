//制品库声明
ext {
    //声明只用京东的仓库
    def repository = "https://s01.oss.sonatype.org"
    snapshotsArtifactory = "${repository}/content/repositories/snapshots" as String
    releasesArtifactory = "${repository}/service/local/staging/deploy/maven2/" as String
}