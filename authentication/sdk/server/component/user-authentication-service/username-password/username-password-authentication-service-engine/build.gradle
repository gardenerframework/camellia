dependencies {
    api "${fragrans.group}:api-options-common"
    api "${fragrans.group}:enhance-validation-constraints"
    api "${group}:authentication-server-core"
    api "${group}:username-password-authentication-service-core"
}