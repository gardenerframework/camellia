dependencies {
    api "${fragrans.group}:data-domain-common-practice"
    api "${fragrans.group}:data-traits-starter"
    api "${group}:client-data-schema"
    api "${group}:authorization-data-operation-common"
    runtimeOnly "mysql:mysql-connector-java"
}